:root{
  --bg: #050f10;
  --panel: rgba(255,255,255,0.06);
  --panel2: rgba(255,255,255,0.04);
  --text: #eaf6f5;
  --muted: rgba(234,246,245,0.72);
  --border: rgba(234,246,245,0.12);

  --teal: #14b8a6;
  --teal2:#0ea5a4;
  --gold:#fbbf24;

  --shadow: 0 18px 40px rgba(0,0,0,0.45);
  --radius: 18px;
}

*{ box-sizing: border-box; }
html, body{ height: 100%; }
body{
  margin:0;
  font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans", "Helvetica Neue", sans-serif;
  background:
    radial-gradient(1000px 600px at 15% 10%, rgba(20,184,166,0.20), transparent 60%),
    radial-gradient(900px 700px at 85% 15%, rgba(251,191,36,0.12), transparent 60%),
    radial-gradient(800px 600px at 60% 90%, rgba(14,165,164,0.12), transparent 60%),
    var(--bg);
  color: var(--text);
}

a{ color: inherit; text-decoration: none; }
.container{ width: min(1180px, 92vw); margin: 0 auto; }

.topbar{
  position: sticky;
  top: 0;
  z-index: 20;
  backdrop-filter: blur(12px);
  background: rgba(5,15,16,0.55);
  border-bottom: 1px solid var(--border);
}
.topbar__inner{
  display:flex;
  align-items:center;
  justify-content: space-between;
  padding: 14px 0;
  gap: 16px;
}

.brand{ display:flex; align-items:center; gap: 12px; }
.brand__logo{
  width: 40px; height: 40px;
  display:grid; place-items:center;
  border-radius: 12px;
  background: linear-gradient(135deg, rgba(20,184,166,0.95), rgba(251,191,36,0.55));
  box-shadow: var(--shadow);
  font-weight: 800;
  color: #041010;
}
.brand__name{ font-weight: 800; letter-spacing: 0.6px; }
.brand__tag{ font-size: 12px; color: var(--muted); margin-top: 2px; }

.nav{ display:flex; gap: 14px; flex-wrap: wrap; }
.nav__link{
  padding: 10px 12px;
  border: 1px solid transparent;
  border-radius: 999px;
  color: var(--muted);
}
.nav__link:hover{
  border-color: var(--border);
  color: var(--text);
  background: rgba(255,255,255,0.04);
}

.hero{
  margin: 26px 0 18px;
  display:grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 18px;
  align-items: start;
}
.hero h1{ margin: 0 0 10px; font-size: clamp(24px, 3.3vw, 38px); line-height: 1.1; }
.hero p{ margin: 0 0 12px; color: var(--muted); }
.hero__badges{ display:flex; gap: 8px; flex-wrap: wrap; }
.badge{
  border: 1px solid var(--border);
  background: rgba(255,255,255,0.04);
  padding: 8px 10px;
  border-radius: 999px;
  font-size: 12px;
  color: rgba(234,246,245,0.86);
}

.hero__panel{ display:grid; gap: 12px; }

.layout{
  display:grid;
  grid-template-columns: 1.3fr 0.7fr;
  gap: 18px;
  margin: 16px 0 44px;
}
.col{ min-width: 0; }
.col--side{ display:flex; flex-direction: column; gap: 18px; }

.card{
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: linear-gradient(180deg, rgba(255,255,255,0.07), rgba(255,255,255,0.03));
  box-shadow: var(--shadow);
}
.card__header{
  padding: 16px 16px 10px;
  border-bottom: 1px solid rgba(234,246,245,0.10);
}
.card__header h2{ margin: 0 0 6px; font-size: 16px; letter-spacing: 0.2px; }
.card__header p{ margin:0; font-size: 13px; color: var(--muted); }

.card--mini{ padding: 14px 16px; background: linear-gradient(180deg, rgba(20,184,166,0.12), rgba(255,255,255,0.03)); }

.muted{ color: var(--muted); }

.grid{
  padding: 14px 16px 16px;
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}
.field{ display:flex; flex-direction: column; gap: 7px; }
.field span{ font-size: 12px; color: rgba(234,246,245,0.85); }
.field input, .field select{
  width: 100%;
  padding: 12px 12px;
  border-radius: 12px;
  border: 1px solid rgba(234,246,245,0.12);
  background: rgba(4,10,10,0.55);
  color: var(--text);
  outline: none;
}
.field input:focus, .field select:focus{
  border-color: rgba(20,184,166,0.6);
  box-shadow: 0 0 0 4px rgba(20,184,166,0.10);
}

.actions{
  padding: 0 16px 16px;
  display:flex;
  gap: 10px;
  flex-wrap: wrap;
}
.btn{
  padding: 12px 14px;
  border-radius: 12px;
  border: 1px solid rgba(234,246,245,0.14);
  background: rgba(255,255,255,0.04);
  color: var(--text);
  cursor: pointer;
}
.btn:hover{ background: rgba(255,255,255,0.07); }
.btn--primary{
  border-color: rgba(20,184,166,0.55);
  background: linear-gradient(135deg, rgba(20,184,166,0.85), rgba(14,165,164,0.55));
  color: #041010;
  font-weight: 800;
}
.btn--primary:hover{ filter: brightness(1.06); }
.btn--ghost{ color: rgba(234,246,245,0.85); }

.report{
  padding: 16px;
  line-height: 1.35;
}
.report.empty{
  color: rgba(234,246,245,0.7);
  background: rgba(255,255,255,0.02);
}
.section{
  margin: 14px 0;
  padding: 12px;
  border-radius: 14px;
  border: 1px solid rgba(234,246,245,0.10);
  background: rgba(255,255,255,0.03);
}
.section h3{
  margin: 0 0 10px;
  font-size: 14px;
  letter-spacing: 0.2px;
  display:flex;
  align-items:center;
  justify-content: space-between;
}
.kv{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}
.kv .item{
  padding: 10px;
  border-radius: 12px;
  border: 1px solid rgba(234,246,245,0.10);
  background: rgba(0,0,0,0.16);
}
.kv .label{ font-size: 12px; color: var(--muted); margin-bottom: 6px; }
.kv .value{ font-weight: 700; }

.pills{ display:flex; gap: 8px; flex-wrap: wrap; }
.pill{
  padding: 8px 10px;
  border-radius: 999px;
  font-size: 12px;
  border: 1px solid rgba(234,246,245,0.12);
  background: rgba(255,255,255,0.03);
}
.pill--good{ border-color: rgba(20,184,166,0.45); }
.pill--warn{ border-color: rgba(251,191,36,0.45); }
.pill--bad{ border-color: rgba(239,68,68,0.45); }

.tableWrap{ overflow:auto; }
.table{
  width: 100%;
  border-collapse: collapse;
}
.table th, .table td{
  padding: 10px 10px;
  border-top: 1px solid rgba(234,246,245,0.10);
  vertical-align: top;
  font-size: 13px;
}
.table th{ text-align: left; color: rgba(234,246,245,0.85); font-size: 12px; }
.table td{ color: rgba(234,246,245,0.92); }
.table tbody tr:hover td{ background: rgba(255,255,255,0.03); }

.checklist, .qa{
  list-style: none;
  padding: 12px 16px 16px;
  margin: 0;
  display:flex;
  flex-direction: column;
  gap: 10px;
}
.checklist li, .qa li{
  padding: 10px 10px;
  border-radius: 12px;
  border: 1px solid rgba(234,246,245,0.10);
  background: rgba(0,0,0,0.16);
  color: rgba(234,246,245,0.92);
}

.progress{
  height: 10px;
  border-radius: 999px;
  border: 1px solid rgba(234,246,245,0.12);
  background: rgba(0,0,0,0.22);
  overflow: hidden;
}
.progress__bar{
  height: 100%;
  width: 0%;
  background: linear-gradient(90deg, rgba(20,184,166,0.95), rgba(251,191,36,0.85));
  transition: width 700ms ease;
}
.mini{ display:flex; flex-direction: column; gap: 10px; }
.mini__title{ font-size: 12px; color: rgba(234,246,245,0.85); }
.mini__value{ font-weight: 800; color: rgba(234,246,245,0.92); }

.footer{
  border-top: 1px solid rgba(234,246,245,0.10);
  padding: 18px 0 36px;
}
.footer__inner{ display:flex; justify-content: space-between; gap: 10px; flex-wrap: wrap; }

@media (max-width: 980px){
  .hero{ grid-template-columns: 1fr; }
  .layout{ grid-template-columns: 1fr; }
  .grid{ grid-template-columns: 1fr; }
}